# No authentication, no encryption (PLAINTEXT). Test/lab listeners only.
# The empty block's PRESENCE selects this method.
#
# Usable in any Kafka slot (source, clusterLink.source, clusterLink.destination).
unauthenticated_plaintext: {}
