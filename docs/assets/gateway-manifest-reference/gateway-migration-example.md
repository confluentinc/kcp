# Gateway migration example

The fully-annotated, ready-to-copy `gateway-migration.yaml` manifest — every
field, with comments. For the field-by-field meaning of everything here, see
the [gateway manifest reference](README.md).

Also available as a plain file at
[`gateway-examples/gateway-migration.yaml`](../gateway-examples/gateway-migration.yaml)
— that link downloads the raw file rather than opening it in-browser, since
it's YAML, not a doc page.

```yaml
--8<-- "docs/assets/gateway-examples/gateway-migration.yaml"
```
