apiVersion: v1
kind: Secret
metadata:
  name: destination-plain-credential
  namespace: confluent
type: Opaque
stringData:
  plain-users.json: |
    {
      "admin": "admin-secret",
      "testuser": "testpassword"
    }
  plain.txt: |
    username=admin
    password=admin-secret
  plain-interbroker.txt: |
    username=admin
    password=admin-secret
