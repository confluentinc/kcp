clusters:
  - id: test-kafka-jmx-auth
    bootstrap_servers:
      - localhost:9097
    auth_method:
      unauthenticated_plaintext:
        use: true
    jolokia:
      endpoints:
        - http://localhost:8779/jolokia
      auth:
        username: monitorUser
        password: monitorPass
    metadata:
      environment: test
      location: docker-local-jmx-auth
