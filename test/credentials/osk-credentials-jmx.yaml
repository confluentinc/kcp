clusters:
  - id: test-kafka-jmx
    bootstrap_servers:
      - localhost:9096
    auth_method:
      unauthenticated_plaintext:
        use: true
    jmx:
      type: jolokia
      endpoints:
        - http://localhost:8778/jolokia
    metadata:
      environment: test
      location: docker-local-jmx
