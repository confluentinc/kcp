clusters:
  - id: disabled-cluster
    bootstrap_servers:
      - localhost:9092
    auth_method:
      sasl_scram:
        use: false
      tls:
        use: false
      unauthenticated_plaintext:
        use: false
