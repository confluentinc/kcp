clusters:
  - id: source
    bootstrap_servers: ["localhost:19094"]    # SSL host listener
    insecure_skip_tls_verify: true
    auth_method:
      unauthenticated_tls:
        use: true
        ca_cert: ./certs/ca.crt
