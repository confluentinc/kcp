// JAAS login config for the Basic-auth Connect worker. The realm name must be
// "KafkaConnect" — that is what BasicAuthSecurityRestExtension looks up.
KafkaConnect {
  org.apache.kafka.connect.rest.basic.auth.extension.PropertyFileLoginModule required
  file="/etc/connect/rest-credentials.properties";
};
