clusters:
  - id: apache-kafka-jmx-auth
    bootstrap_servers:
      - localhost:9096
    auth_method:
      unauthenticated_plaintext:
        use: true
    jolokia:
      endpoints:
        - http://localhost:8779/jolokia
      auth:
        username: monitorUser
        password: monitorPass
    metadata:
      environment: test
      location: apache-kafka-scan
